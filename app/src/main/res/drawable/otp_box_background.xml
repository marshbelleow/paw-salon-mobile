<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background Color -->
    <solid android:color="@color/very_lightblue" />

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="@color/very_lightblue" />

    <!-- Corner Radius -->
    <corners android:radius="8dp" />

    <!-- Padding -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="5dp"
        android:bottom="5dp" />
</shape>
